# pre-commit:
#   commands:
#     lint:
#       glob: "*.{ts,tsx}"
#       run: bunx biome check --write --no-errors-on-unmatched {staged_files} && git add {staged_files}

commit-msg:
  scripts:
    "commitlint":
      runner: bash
